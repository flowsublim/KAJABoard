<style>
  /* ================= TEMA "MODERN SAAS" & GLASSMORPHISM ================= */
  :root {
    --bs-primary: #3b82f6; --bs-success: #10b981; --bs-danger:  #ef4444;       
    --bs-info: #06b6d4; --bs-warning: #f59e0b; --bs-secondary: #64748b;     
    --bs-body-bg: #f8fafc; --bs-body-color: #334155;
    --bs-font-sans-serif: 'Inter', sans-serif; 
  }

  body { background-color: #f1f5f9; color: var(--bs-body-color); font-family: var(--bs-font-sans-serif); letter-spacing: 0.2px; }

  /* --- Kotak/Card & Modal --- */
  .card, .modal-content {
    border: none !important; border-radius: 12px; background-color: #ffffff;
    box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.05), 0 4px 6px -2px rgba(0, 0, 0, 0.025);
  }
  .modal-header { border-bottom: 1px solid #f1f5f9; border-top-left-radius: 12px; border-top-right-radius: 12px; }
  
  .form-control, .form-select { border-radius: 8px; border: 1px solid #cbd5e1 !important; background-color: #f8fafc; padding: 0.5rem 0.75rem; font-size: 0.9rem; color: #334155; }
  .form-control:focus, .form-select:focus { background-color: #ffffff; border-color: #93c5fd !important; box-shadow: 0 0 0 0.25rem rgba(59, 130, 246, 0.25); }
  .btn { border-radius: 8px; font-weight: 600; letter-spacing: 0.3px; transition: 0.2s; }
  .btn:hover { transform: translateY(-1px); }

  .table { color: var(--bs-body-color); width: 100% !important; }
  .table > :not(caption) > * > * { padding: 0.75rem 0.75rem !important; border-bottom-color: #e2e8f0; vertical-align: middle; }
  .table-responsive { max-height: 60vh; overflow-y: auto; border: 1px solid #e2e8f0; border-radius: 8px; }
  thead th { font-size: 0.8rem !important; text-transform: uppercase; letter-spacing: 0.5px; font-weight: 700; background-color: #f8fafc !important; color: #64748b !important; position: sticky !important; top: 0; z-index: 10; box-shadow: inset 0 -2px 0 rgba(0,0,0,0.05); height: 45px !important; line-height: 20px !important; }
  tbody td { font-size: 0.85rem !important; }

  .box-item { border: 1px solid #e2e8f0; border-radius: 12px; padding: 12px; margin-bottom: 10px; background-color: #f8fafc; }

  /* ================= CSS GLASSMORPHISM & SIDEBAR ================= */
  .glass-sidebar {
    width: 240px !important;
    min-width: 240px !important;
    max-width: 240px !important;
    background: rgba(15, 23, 42, 0.95);
    backdrop-filter: blur(15px); -webkit-backdrop-filter: blur(15px);
    border: 1px solid rgba(255, 255, 255, 0.08); 
    border-radius: 24px;
    margin: 16px 0 16px 16px; 
    transition: all 0.3s ease;
    height: calc(100vh - 32px);
    overflow-y: auto;
  }
  
  /* Sembunyikan scrollbar di sidebar agar rapi */
  .glass-sidebar::-webkit-scrollbar { display: none; }
  .glass-sidebar { -ms-overflow-style: none; scrollbar-width: none; }
  
  .glass-sidebar .nav-link { color: #cbd5e1; padding: 12px 16px; font-weight: 500; border-radius: 12px; margin-bottom: 6px; transition: 0.2s; }
  .glass-sidebar .nav-link:hover { background: rgba(255, 255, 255, 0.1); color: #fff; transform: translateX(4px); }
  .glass-sidebar .nav-link.active { background: var(--bs-primary); color: #fff !important; box-shadow: 0 4px 10px rgba(59, 130, 246, 0.4); }

  .glass-header {
    background: rgba(255, 255, 255, 0.85);
    backdrop-filter: blur(12px); -webkit-backdrop-filter: blur(12px);
    border: 1px solid rgba(255, 255, 255, 0.6); 
    border-radius: 20px;
    position: sticky; top: 16px; z-index: 1030; /* 💡 KUNCI: Disamakan posisinya dengan margin-top sidebar */
    margin: 16px; /* 💡 KUNCI: Jarak 16px ke semua sisi agar proporsional dan lebih lebar! */
  }
  
  .main-content { height: 100vh; overflow-y: auto; overflow-x: hidden; }
  .main-content::-webkit-scrollbar { width: 6px; }
  .main-content::-webkit-scrollbar-thumb { background: #cbd5e1; border-radius: 10px; }

  /* ================= RESPONSIVE MOBILE FIX & COMPACT UI ================= */
  @media (max-width: 991px) {
    .glass-header { 
      flex-wrap: nowrap !important; 
      padding: 8px 12px !important; 
      margin: 0 0 10px 0 !important; 
      border-radius: 0 0 12px 12px !important;
      border-top: none; border-left: none; border-right: none;
      position: sticky; top: 0; z-index: 1030;
    }
    .glass-header h4 { font-size: 1.1rem !important; }
    .glass-header .judul-subtitle { display: none !important; } 
    
    .btn-pindah-modul { width: 38px !important; height: 38px !important; padding: 0 !important; }
    .btn-pindah-modul::after { display: none !important; } 
    .teks-pindah-modul { display: none !important; } /* Paksa hilang 100% di HP */

    body { font-size: 0.85rem !important; }
    .main-content { padding-bottom: 75px !important; } /* Ruang aman untuk nav bawah */
    
    .card { padding: 12px !important; border-radius: 10px !important; }
    .card h5 { font-size: 1rem !important; margin-bottom: 10px !important; }
    
    .table > :not(caption) > * > * { padding: 0.4rem !important; }
    thead th { font-size: 0.75rem !important; height: auto !important; line-height: 1.2 !important; padding-top: 8px !important; padding-bottom: 8px !important; }
    tbody td { font-size: 0.8rem !important; vertical-align: middle; }
    
    .form-control, .form-select { font-size: 0.85rem !important; padding: 0.35rem 0.6rem !important; border-radius: 6px !important; height: auto !important; }
    .btn { font-size: 0.85rem !important; padding: 0.35rem 0.7rem !important; border-radius: 6px !important; }
    .badge { font-size: 0.75rem !important; padding: 0.35em 0.5em !important; }
  }

  /* ================= CSS TOAST NOTIFICATION ================= */
  .custom-toast {
    min-width: 280px;
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px); -webkit-backdrop-filter: blur(10px);
    border-left: 5px solid var(--bs-success);
    box-shadow: 0 10px 25px rgba(0,0,0,0.15);
    border-radius: 8px;
    padding: 12px 20px;
    text-align: center;
    font-weight: 700;
    color: #334155;
    animation: slideDownFade 0.3s ease-out forwards;
    margin-bottom: 10px;
    font-size: 0.9rem;
  }
  .custom-toast.toast-error { border-left-color: var(--bs-danger); }
  .custom-toast.toast-warning { border-left-color: var(--bs-warning); }
  .custom-toast.toast-info { border-left-color: var(--bs-primary); }
  .custom-toast.fade-out { animation: slideUpFade 0.3s ease-in forwards; }
  
  @keyframes slideDownFade { from { opacity: 0; transform: translateY(-20px); } to { opacity: 1; transform: translateY(0); } }
  @keyframes slideUpFade { from { opacity: 1; transform: translateY(0); } to { opacity: 0; transform: translateY(-20px); } }

  /* ================= CSS KHUSUS CETAK PENJUALAN ================= */
  #area-print-penjualan { font-family: Arial, Helvetica, sans-serif; color: #000; line-height: 1.4; padding: 20px; }
  #area-print-penjualan table { width: 100%; border-collapse: collapse; margin-bottom: 1rem; }
  #area-print-penjualan th, #area-print-penjualan td { padding: 6px 10px; }
  
  @media print {
    .glass-sidebar, .glass-header, #box-filter-penjualan, #tab-po, #tab-sj, #tab-inv, #tab-piutang, #notif-global, .modal-backdrop, .d-lg-none.fixed-bottom { display: none !important; }
    body { margin: 0 !important; padding: 0 !important; background-color: #ffffff !important; }
    #modalPreviewCetak { display: block !important; position: absolute !important; left: 0; top: 0; width: 100%; height: auto !important; margin: 0 !important; padding: 0 !important; overflow: visible !important; }
    .modal-dialog, .modal-content { position: static !important; width: 100% !important; max-width: 100% !important; border: none !important; box-shadow: none !important; border-radius: 0 !important; }
    .modal-header, .modal-footer, .print-hide { display: none !important; }
    .modal-body { padding: 0 !important; }
    .main-content { overflow: visible !important; height: auto !important; }
  }
  
  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(-10px); }
    to { opacity: 1; transform: translateY(0); }
  }

  /* ================= CSS KHUSUS FINANCE ================= */
  .card h4 { letter-spacing: -0.4px; }
  #tab-jurnal .box-item { min-height: 92px; }
  .table-responsive .alert { border-radius: 8px; }
  .badge { font-weight: 700; }
  .modal-xl { max-width: 1100px; }

  @media print {
    #tab-dashboard, #tab-jurnal, #tab-piutang, #tab-hutang, #tab-aruskas, #tab-labarugi, #tab-neraca, #tab-cogm, #tab-setting { display: block !important; }
  }

  /* v0.6 modal finance dibuat lebih rapih dan seimbang */
  .finance-modal-form .form-label { font-weight: 700; color: #334155; margin-bottom: .35rem; }
  .finance-modal-form .form-control,
  .finance-modal-form .form-select { min-height: 44px; border-radius: 11px; }
  .finance-modal-form .small.text-muted { margin-top: -4px; }
  #modalFinance .modal-dialog { max-width: 920px; }
  @media (max-width: 768px) { #modalFinance .modal-dialog { max-width: calc(100% - 1rem); } }
</style>